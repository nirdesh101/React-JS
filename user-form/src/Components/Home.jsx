import React from "react";

import FormFill from "./FormFill";

function Home({ userData, setUserData }) {
  return <FormFill userData={userData} setUserData={setUserData} />;
}

export default Home;
